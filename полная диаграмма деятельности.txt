@startuml Activity Main User Flow

title "Диаграмма деятельности: Основной поток работы пользователя"

start

if (У пользователя есть учётная запись?) then (Да)
  :Вход в систему с логином/паролем;
else (Нет)
  :Регистрация нового аккаунта;
endif

if (Аутентификация успешна?) then (Нет)
  :Отображение ошибки аутентификации;
  stop
else (Да)
  :Переход на главную страницу;
  
  if (Выбрано действие пользователя == Анализ файлов) then (Да)
    :Загрузка файла requirements.txt;
    :Запуск проверки зависимостей;
    :Просмотр результатов анализа;
    if (Требуется экспорт?) then (Да)
      :Экспорт в PDF;
    endif
    
  else if (Выбрано действие == Управление профилем) then (Да)
    :Переход в личный кабинет;
    
    if (Действие == API ключ) then (Да)
      :Просмотр или генерация нового API ключа;
    else (Нет)
      :Изменение персональных данных;
    endif
    
  else (Нет)
    :Выход с инвалидацией токенов;
  endif
  
endif

stop

@enduml