```c
===============================================================================
СЕТЕВАЯ МНОГОПОТОЧНАЯ СИСТЕМА ВАННЫХ КОМНАТ — ТЕОРИЯ И РАЗБОР
===============================================================================
Цель: объяснить принципы работы клиент-серверной многопоточной программы
управления доступом к разделяемому ресурсу (ванная комната) с использованием:
- сетевых сокетов TCP,
- многопоточности (pthread),
- межпроцессной/межпоточной синхронизации,
- механизма приоритетов ("стрик").
-------------------------------------------------------------------------------
1. ОБЩАЯ АРХИТЕКТУРА ПРОГРАММЫ
-------------------------------------------------------------------------------
Программа моделирует доступ студентов (мужчин и женщин) к общей ванной комнате
в общежитии университета через сеть. Архитектура:
• **Сервер** (server.c): хранит глобальное состояние ванны, обеспечивает
синхронизацию между клиентами, управляется синхронизационными примитивами.
• **Клиенты** (client.c): запускают потоки-студентов, которые подключаются к
серверу по TCP, отправляют запросы на вход/выход и получают обновления.
Основные правила:
• В ванной одновременно может находиться до CAPACITY человек.
• Внутри могут быть только люди одного пола (никаких смешанных групп).
• При входе определяется приоритет через "стрик": если подряд зашло N человек
одного пола, следующий приоритет получает другой пол.
• Если представителей приоритетного пола нет, стрик сбрасывается.
• Выводится статистика: среднее время ожидания, загрузка, распределение по полу.
В отличие от многопроцессной версии, здесь:
- Клиенты запускают **потоки**, а не процессы.
- Состояние хранится **на сервере**, а не в разделяемой памяти.
- Синхронизация происходит **через сеть** с помощью сообщ

2. КЛЮЧЕВЫЕ МЕХАНИЗМЫ ОПЕРАЦИОННОЙ СИСТЕМЫ
-------------------------------------------------------------------------------
Для реализации сетевой версии используются следующие системные вызовы:
=== 2.1 === socket() ===
#include <sys/socket.h>
int socket(int domain, int type, int protocol);
Назначение:
Создает новый сокет для сетевого взаимодействия.
Параметры:
- domain: AF_INET (IPv4)
- type: SOCK_STREAM (TCP - надежная доставка)
- protocol: 0 (автоматический выбор протокола)
Возвращаемое значение:
Файловый дескриптор (socket_fd), который используется для дальнейших операций.
=== 2.2 === bind() ===
#include <sys/socket.h>
int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
Назначение:
Связывает сокет с локальным адресом и портом.
=== 2.3 === listen() ===
int listen(int sockfd, int backlog);
Назначение:
Переводит сокет в режим прослушивания.
backlog: максимальное количество ожидающих подключений.
 * backlog и его роль в сетевой ванной комнате
 *
 * Вопрос: "Что в моем случае ограничивает backlog? Поток студентов?"
 *
 * ОТВЕТ: НЕТ.
 * Параметр `backlog`, передаваемый в функцию `listen(server_fd, BACKLOG)`
 * на сервере (в вашем `server.c`), **не ограничивает** количество
 * потоков-студентов, создаваемых на клиентской стороне (`client.c`).
 *
 * ========================================================================
 * 1. НАЗНАЧЕНИЕ backlog
 * ========================================================================
 * `backlog` определяет **максимальное количество входящих соединений,
 * ожидающих в ОЧЕРЕДИ ПОДКЛЮЧЕНИЙ** операционной системы для указанного
 * слушающего сокета (`server_fd`) на **стороне сервера**.
 *
 * typedef enum {
 *     MSG_JOIN,
 *     MSG_LEAVE,
 *     MSG_UPDATE,
 *     MSG_STATS
 * } msg_type_t;
 *
 * typedef struct {
 *     msg_type_t type;
 *     // ... остальные поля ...
 * } msg_t;
 *
 * int main() {
 *     // ... инициализация ...
 *     int server_fd = socket(AF_INET, SOCK_STREAM, 0);
 *
 *     // bind, setsockopt и т.д.
 *
 *     // listen устанавливает размер очереди ожидания подключений
 *     if (listen(server_fd, BACKLOG) < 0) { // BACKLOG = 10 в вашем коде
 *         perror("listen");
 *         exit(EXIT_FAILURE);
 *     }
 *     printf("Сервер запущен на порту %d\n", PORT);
 *
 *     while (1) {
 *         // accept извлекает *одно* соединение из очереди listen
 *         int new_socket = accept(server_fd, (struct sockaddr *)&address, &addrlen);
 *         if (new_socket < 0) {
 *             perror("accept");
 *             exit(EXIT_FAILURE);
 *         }
 *
 *         // Создается новый поток для обработки *уже установленного* соединения
 *         int *fd_ptr = malloc(sizeof(int));
 *         *fd_ptr = new_socket;
 *         pthread_t thread;
 *         pthread_create(&thread, NULL, handle_client, fd_ptr);
 *         pthread_detach(thread); // Поток не будет ждаться
 *     }
 *     // ...
 * }
 *
 * ========================================================================
 * 2. КАК РАБОТАЕТ backlog
 * ========================================================================
 * - Когда клиент (например, поток студента в `client.c`) вызывает `connect()`,
 *   он пытается установить TCP-соединение с сервером.
 * - Если сервер вызывает `accept()`, он извлекает *одно* соединение из
 *   очереди, созданное `listen()`.
 * - Если очередь, размер которой определяется `backlog`, заполнена,
 *   и приходит новое подключение, ОС может отклонить его
 *   (обычно с ошибкой "Connection refused" на стороне клиента).
 * - В вашем `client.c` потоки студентов запускаются с задержкой:
 *   for (int i = 0; i < total_students; i++) {
 *       // ...
 *       pthread_create(&threads[i], NULL, student_thread, id);
 *       usleep(100000); // Задержка 0.1 секунды между запусками потоков
 *   }
 *   Это означает, что **не все** потоки-студенты одновременно пытаются
 *   подключиться к серверу. Подключения происходят постепенно.
 *
 * ========================================================================
 * 3. ПОТОКИ СТУДЕНТОВ НА КЛИЕНТЕ
 * ========================================================================
 * - Каждый поток в `client.c` (`student_thread`) создает свой собственный
 *   сокет, подключается к серверу, обменивается сообщениями (`msg_t`)
 *   через `send()` и `recv()`, и закрывает свой сокет.
 * - Количество *одновременно работающих* потоков-студентов ограничено
 *   логикой вашей программы (например, общим количеством студентов),
 *   но НЕ параметром `backlog` на сервере.
 *
 * void* student_thread(void *arg) {
 *     // ...
 *     int sock = 0;
 *     struct sockaddr_in serv_addr;
 *     // ...
 *     if ((sock = socket(AF_INET, SOCK_STREAM, 0)) < 0) {
 *         printf("Ошибка создания сокета\n");
 *         return NULL;
 *     }
 *
 *     // Подготовка адреса сервера
 *     serv_addr.sin_family = AF_INET;
 *     serv_addr.sin_port = htons(PORT);
 *     inet_pton(AF_INET, server_ip, &serv_addr.sin_addr);
 *
 *     // Попытка подключения -> может быть поставлена в очередь listen
 *     if (connect(sock, (struct sockaddr*)&serv_addr, sizeof(serv_addr)) < 0) {
 *         printf("Подключение не удалось\n");
 *         return NULL;
 *     }
 *
 *     // Обмен сообщениями
 msg;
 *     msg.type = MSG_JOIN;
 *     // ...
 *     send(sock, &msg, sizeof(msg), 0);
 *     recv(sock, &msg, sizeof(msg), 0);
 *     // ...
 *     sleep(wash_time);
 *     msg.type = MSG_LEAVE;
 *     send(sock, &msg, sizeof(msg), 0);
 *
 *     close(sock); // Закрытие сокета клиента
 *     // ...
 *     return NULL;
 * }
 *
 * ========================================================================
 * 4. ВЫ * ========================================================================
 * `backlog` в `listen()` ограничивает **размер системной очереди
 * ожидающих подключений** на **сервере**. Он не влияет напрямую
 * на количество или поведение потоков-студентов на **клиенте**.
 * В вашем случае, благодаря задержке между запуском потоков-студентов,
 * очередь `backlog` вряд ли будет заполняться до предела, и `backlog`
 * не станет узким местом.
 */
=== 2.4 === accept() ===
int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen);
Назначение:
Принимает входящее подключение. Блокируется до тех пор, пока клиент не подключится.
Возвращает новый файловый дескриптор для взаимодействия с конкретным клиентом.
=== 2.5 === connect() ===
int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
Назначение:
Подключается к сервер адресу.
=== 2.6 === send() / recv() ===
ssize_t send(int sockfd, const size_t len, int recv(int sockfd, void *buf, size_t len, int flags);
Назначение:
Отправка и получение данных по TCP-соединению.
=== 2.7 === pthread_create() ===
#include <pthread.h>
int pthread_create(pthread_t *thread, const pthread_attr_t *attr,
                   void *(*start_routine) (void *), void *arg);
Назначение:
Создает новый поток выполнения.
=== 2.8/unlock ===
Блокирует/разблокирует доступ к разделяеме.
Только один поток может держать мьютекс в каждый момент времени.
=== 2.9 === pthread_cond_wait() ===
- Освобождает мьютекс
- Переводит поток в состояние ожидания
- Будится только при вызове pthread_cond_broadcast()
=== 2.10 === pthread_cond_broadcast() ===
Разбуживает **все** потоки, ожидающие на условной переменной.
-------------------------------------------------------------------------------
3. СТРУКТУРА СООБЩЕНИЙ (msg_t)
-------------------------------------------------------------------------------
typedef struct {
msg_type_t type;          // тип сообщения (MSG_JOIN, MSG_LEAVE, MSG_UPDATE, MSG_STATS)
int id;                   // ID студента
int gender;               // 0=MALE, 1=FEMALE
long wait_start;          // время начала ожидания (от клиента)
long bath_time;           // время в ванной (от клиента)
int occupied;             // сколько сейчас занято (для обновлений)
int capacity;             // вместимость ванны (для обновлений)
int streak_gender;        // кто имеет приоритет (-1 = не определён) (для обновлений)
int streak_used;          // сколько подряд уже вошло (для обновлений)
int remaining_male;       // сколько мужчин осталось (для обновлений)
int remaining_female;     // сколько женщин осталось (для обновлений)
long total_wait_time;     // суммарное время ожидания всех (для статистики)
long total_bath_time;     // суммарное время пребывания (для статистики)
int entered_count;        // всего вошло (для статистики)
int male_entered;         // статистика по мужчинам (для статистики)
int female_entered;       // статистика по женщинам (для статистики)
} msg_t;
Эта структура используется для сериализации информации между клиентом и сервером.
-------------------------------------------------------------------------------
4. СТРУКТУРА bath_server_t — ЦЕНТРАЛЬНЫЙ ОБЪЕКТ СИСТЕМЫ (только на сервере)
-------------------------------------------------------------------------------
typedef struct {
pthread_mutex_t mutex;            // основной мьютекс для синхронизации
pthread_cond_t cond;              // условная переменная для ожидания
int capacity;                     // вместимость ванны
int occupied;                     // сколько сейчас занято
bath_state_t bath_gender;         // кто сейчас внутри (EMPTY, MEN_INSIDE, WOMEN_INSIDE)
int streak_gender;                // кто имеет приоритет (-1 = не определён)
int streak_used;                  // сколько подряд уже вошло
int streak_limit;                 // максимальная длина стрика (по умолчанию 5)
int remaining_male, remaining_female; // сколько мужчин/женщин ожидают
long total_wait_time;             // суммарное время ожидания всех
long total_bath_time;             // суммарное время пребывания
int entered_count;                // всего вошло
int male_entered, female_entered; // статистика по полу
} bath_server_t;
Важно: вся эта структура находится в памяти **сервера** и защищена мьютексом.
-------------------------------------------------------------------------------
5. ЛОГИКА РАБОТЫ СЕРВЕРА (handle_client)
-------------------------------------------------------------------------------
=== Шаг 1: Принятие подключения ===
- accept() возвращает новый сокет для взаимодействия с клиентом
- Создается новый поток (pthread_create) для обслуживания клиента
- Поток вызывает handle_client
=== Шаг 2: Цикл получения сообщений ===
while (recv(client_fd, &msg, sizeof(msg), 0) > 0) {
=== Шаг 3: Блокировка основного мьютекса ===
pthread_mutex_lock(&bath_s.mutex)
=== Шаг 4: Обработка сообщения по типу ===
- MSG_JOIN:
- Увеличение счетчика ожидающих (remaining_male/female)
- Цикл проверки возможности входа (см. раздел 6)
- Если можно войти: обновление состояния, отправка MSG_UPDATE клиенту
- Если нельзя: pthread_cond_wait()
- MSG_LEAVE:
- Уменьшение occupied, обновление статистики
- Если occupied == 0, сброс bath_gender
- pthread_cond_broadcast() для пробуждения ожидающих
- MSG_STATS:
- Заполнение полей статистики и отправка клиенту
=== Шаг 5: Разблокировка мьютекса ===
pthread_mutex_unlock(&bath_s.mutex)
=== Шаг 6: Закрытие соединения ===
Когда recv возвращает <= 0, клиент отключился. Закрываем сокет.
-------------------------------------------------------------------------------
6. ЛОГИКА ВХОДА В ВАННУ (на сервере, внутри MSG_JOIN)
-------------------------------------------------------------------------------
while (1) {
int can_enter = 0;
if (bath_s.occupied >= bath_s.capacity) {
can_enter = 0;  // ванна переполнена
} else if (bath_s.occupied == 0) {
// Ванна пуста
if (bath_s.streak_gender == -1) {
can_enter = 1;  // любой может войти
} else {
int allowed = (bath_s.streak_used < bath_s.streak_limit) ?
bath_s.streak_gender : !bath_s.streak_gender;
if (msg.gender == allowed) {
can_enter = 1;
} else {
// Сброс стрика, если приоритетного пола нет
if ((allowed == 0 && bath_s.remaining_male == 0) ||
(allowed == 1 && bath_s.remaining_female == 0)) {
bath_s.streak_gender = -1;
bath_s.streak_used = 0;
can_enter = 1;
}
}
}
} else {
// Ванна занята
if ((bath_s.bath_gender == MEN_INSIDE && msg.gender != 0) ||
(bath_s.bath_gender == WOMEN_INSIDE && msg.gender != 1)) {
can_enter = 0;  // нельзя входить, если пол не совпадает
} else {
int allowed = (bath_s.streak_used < bath_s.streak_limit) ?
bath_s.streak_gender : !bath_s.streak_gender;
can_enter = (msg.gender == allowed);
}
}
if (can_enter) break;
pthread_cond_wait(&bath_s.cond, &bath_s.mutex);  // ждем пробуждения
}
=== Шаг 5: Вход в ванну (после break) ===
- Обновление времени ожидания
- Увеличение bath_s.occupied
- Установка bath_s.bath_gender при первом входе
- Уменьшение счетчика ожидающих (remaining_male/female)
- Обновление стрика (streak_gender, streak_used)
- Подготовка и отправка MSG_UPDATE клиенту
- Цикл завершается, управление возвращается в основной цикл обработки
-------------------------------------------------------------------------------
7. ЛОГИКА РАБОТЫ КЛИЕНТА (student_thread)
-------------------------------------------------------------------------------
=== Шаг 1: Инициализация ===
- Генерация пола: случайно (rand() % 100 < 50)
- Время мытья: от 1 до 4 секунд (1 + rand() % 4)
- Создание TCP-сокета
- Подключение к серверу по адресу из config.txt или localhost
=== Шаг 2: Отправка запроса на вход ===
- msg.type = MSG_JOIN
- msg.gender = сгенерированный пол
- msg.wait_start = time(NULL)
- send(sock, &msg, sizeof(msg), 0)
=== Шаг 3: Ожидание разрешения на вход ===
- recv(sock, &msg, sizeof(msg), 0) — получаем MSG_UPDATE
- Вывод статуса "ВХОД"
=== Шаг 4: Пребывание в ванне ===
sleep(wash_time);  // имитация времени мытья
=== Шаг 5: Отправка уведомления о выходе ===
- msg.type = MSG_LEAVE
- msg.bath_time = затраченное время
- send(sock, &msg, sizeof(msg), 0)
=== Шаг 6: Закрытие соединения ===
close(sock)
=== Шаг 7: Вывод завершения ===
Выводит информацию о завершении работы студента
-------------------------------------------------------------------------------
8. ЗАПРОС СТАТИСТИКИ (в main клиента)
-------------------------------------------------------------------------------
После завершения всех потоков-студентов:
- Подключаемся к серверу снова
- Отправляем msg.type = MSG_STATS
- Получаем заполненную структуру с серверной статистикой
- Выводим результаты
-------------------------------------------------------------------------------
9. СТРИК-МЕХАНИЗМ (FAIRNESS POLICY)
-------------------------------------------------------------------------------
Цель: предотвратить ситуацию, когда один пол постоянно занимает ванну.
Правила (реализованы на сервере. Если подряд вошло streak_limit студентов одного пола, следующий приоритет
переходит к другому полу.
2. Еслиитетного пола нет в очереди, стрик сбрасывается.
3. При входе нового студента стрик обновляется:
- Если пол совпадает — увеличиваем счетчик
- Иначе — начинаем новый стрик
Пример:
- Заходят 5 мужчин → стрик достигнут
- Теперь приоритет у женщин
- Если женщины есть — они входят
- Если женщин нет — стрик сбрасывается, мужчины могут снова входить
-------------------------------------------------------------------------------
10. ИНИЦИАЛИЗАЦИЯ И ЗАВЕРШЕНИЕ ПРОГРАММЫ
-------------------------------------------------------------------------------
=== server.c — пошагово ===
1. bath_server_init() — инициализируем структуру и синхронизационные объекты
2. socket() + setsockopt() + bind() + listen() — создаем TCP-сервер
3. Бесконечный цикл accept() + pthread_create(handle_client) — принимаем клиентов
4. handle_client() — обрабатывает все сообщения от клиента
5. Сервер работает до принудительной остановки (Ctrl+C)
=== client.c — пошагово ===
1. Проверка аргументов командной строки (число студентов)
2. read_config("config.txt") — читаем IP-адрес сервера
3. Цикл создания потоков (student_thread) для каждого студента
4. pthread_join() для ожидания завершения всех студентов
5. Отправка MSG_STATS и получение финальной статистики
-------------------------------------------------------------------------------
11. ЧАСТЫЕ ОШИБКИ И КАК ИХ ИЗБЕЖАТЬ
-------------------------------------------------------------------------------
❌ Не закрывать сокеты → утечка файловых дескрипторов
✅ Всегда вызывать close() в конце работы с соединением
❌ Забыть pthread_mutex_lock/unlock → race condition
✅ Всегда оборачивать доступ к bath_s в блокировку
❌ Неправильный порядок захвата мьютексов → deadlock
✅ В клиенте нет мьютексов, но на сервере только один: проблем не должно быть
❌ Одинаковые rand() последовательности
✅ Использовать srand(time(NULL) ^ pthread_self()) в каждом потоке
❌ Не обрабатывать ошибки send/recv → падение
✅ Проверять возвращаемые значения
❌ Неправильно форматировать или отправлять структуру → некорректные данные
✅ Отправлять/принимать всю структуру целиком: send(sock, &msg, sizeof(msg), 0)
❌ Забыть config.txt → сервер
✅ Убедиться, что файл существует и содержит правильный IP
❌ Не использовать PTHREAD_MUTEX_INITIALIZER для статических мьютексов
✅ Использовать правильно: pthread_mutex_t lock = PTHREAD_MUTEX_INITIALIZER;
-------------------------------------------------------------------------------
12. КОМПИЛЯЦИЯ И ЗАПУСК
-------------------------------------------------------------------------------
Компиляция сервера:
gcc -o server server.c -lpthread
Компиляция клиента:
gcc -o client client.c -lpthread
Запуск:
1. ./server (на машине, которая будет сервером)
2. Создать config.txt с IP-адресом сервера: echo "127.0.0.1" > config.txt
3. ./client 10 (например, для 10 студентов)
-------------------------------------------------------------------------------
13. ОСОБЕННОСТИ СЕТЕВОГО ВЗАИМОДЕЙСТВИЯ
-------------------------------------------------------------------------------
=== TCP vs UDP ===
- Используется TCP (SOCK_STREAM) — гарантия доставки, порядок сообщений
- UDP (SOCK_DGRAM) не подходит — нужна надежность
=== Сериализация данных ===
- Структура msg_t отправляется как есть (бинарно)
- Это работает, если клиент и сервер компилируются с одинаковым ABI
- На разных архитектурах может потребоваться нормальный протокол (JSON, protobuf)
=== Безопасность ===
- Нет аутентификации клиентов
- Нет шифрования
- Не подходит для production, только для обучения
=== Масштабируемость ===
- Один сокет на клиента
- Один поток на клиента (на сервере)
- Много клиентов -> много потоков -> ограничения ОС
-------------------------------------------------------------------------------
14. ПОТОКОНЕБЕЗОПАСНЫЕ ОПЕРАЦИИ
-------------------------------------------------------------------------------
В клиенте:
- printf() — может вызывать гонки при выводе, но не критично
- rand() — может давать одинаковые числа, но не критично
- socket, connect, send, recv — потокобезопасны на разных сокетах
В сервере:
- ВСЕ обращения к bath_s ДОЛЖНЫ быть защищены мьютексом
- send/recv на разных клиентских сокетах — безопасны
- pthread_cond_wait/broadcast — безопасны при защищенном мьютексом состоянии
-------------------------------------------------------------------------------
15. ЗАКЛЮЧЕНИЕ
-------------------------------------------------------------------------------
Эта программа демонстрирует ключевые концепции:
- Сетевое программирование (TCP-сокеты)
- Многопоточность (pthread)
- Клиент-серверная архитектура
- Синхронизация через сеть
- Справедливое распределение ресурсов (стрик-механизм)
Она может служить шаблоном для других задач, где требуется:
- Распределенная система с централизованным управлением
- Несколько клиентов, общающихся с одним сервером
- Совместное использование ресурса с правилами доступа
Сохраните этот файл как справочник!
-------------------------------------------------------------------------------

```
==============================================================================
ОСНОВЫ SOCKET API — ТЕОРИЯ И РАЗБОР НА ПРИМЕРЕ ПРОГРАММЫ СЕТЕВОЙ ВАННОЙ КОМНАТЫ
================================================================================
Цель: объяснить, как работают сетевые сокеты, используя простые слова и примеры
из реального кода (ваша программа с ванной комнатой).
-------------------------------------------------------------------------------
1. ЧТО ТАКОЕ СОКЕТ?
-------------------------------------------------------------------------------
Сокет — это как "дырка" или "портал", через который ваша программа может
отправлять и получать данные по сети. Представьте себе почтовый ящик:
- Вы положили письмо (данные) — оно ушло в сеть
- Кто-то положил письмо вам — вы его забрали
В программе сокет — это просто число (дескриптор), например: 5, 10, 12.
Как с ним работать:
- Сначала создаете сокет (socket())
- Затем привязываете к адресу (bind() — только для сервера)
- Прослушиваете (listen() — только для сервера)
- Принимаете подключения (accept() — только для сервера)
- Подключаетесь (connect() — только для клиента)
- Отправляете данные (send())
- Получаете данные (recv())
- Закрываете (close())
В вашем коде:
- Сервер: socket() -> bind() -> listen() -> accept() -> send/recv -> close()
- Клиент: socket() -> connect() -> send/recv -> close()
-------------------------------------------------------------------------------
2. АТРИБУТЫ СОКЕТА
-------------------------------------------------------------------------------
Каждый сокет характеризуется тремя вещами:
=== 2.1 === ДОМЕН (domain) ===
Определяет, где будет работать сокет:
- AF_INET — для работы по IP-сети (интернет или локальная сеть)
- AF_UNIX — для общения между процессами на одном компьютере (через файл)
В вашем коде: AF_INET (работа по сети)
=== 2.2 === ТИП (type) ===
Как передаются данные:
- SOCK_STREAM — надежный поток (как труба, данные не теряются, приходят по порядку)
- SOCK_DGRAM — отдельные сообщения (датаграммы, могут потеряться или прийти в разном порядке)
В вашем коде: SOCK_STREAM (надежность важна)
=== 2.3 === ПРОТОКОЛ (protocol) ===
Как именно передаются данные. Обычно 0 — выбирается автоматически.
- TCP — для SOCK_STREAM
- UDP — для SOCK_DGRAM
В вашем коде: 0 (автоматически TCP)
-------------------------------------------------------------------------------
3. ФУНКЦИИ РАБОТЫ С СОКЕТАМИ
-------------------------------------------------------------------------------
=== 3.1 === socket() ===
int socket(int domain, int type, int protocol);
Назначение: создает сокет.
В вашем коде (server.c):
int server_fd;
if ((server_fd = socket(AF_INET, SOCK_STREAM, 0)) == 0) {
perror("socket failed");
exit(EXIT_FAILURE);
}
=== 3.2 === bind() ===
int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
Назначение: привязывает сокет к IP-адресу и порту.
Только для сервера!
В вашем коде:
struct sockaddr_in address;
address.sin_family = AF_INET;
address.sin_addr.s_addr = INADDR_ANY;  // слушать на всех интерфейсах
address.sin_port = htons(PORT);        // PORT = 8989
if (bind(server_fd, (struct sockaddr *)&address, sizeof(address)) < 0) {
perror("bind failed");
exit(EXIT_FAILURE);
}
=== 3.3 === listen() ===
int listen(int sockfd, int backlog);
Назначение: переводит сокет в режим ожидания подключений.
backlog — сколько подключений может ждать в очереди.
В вашем коде:
if (listen(server_fd, BACKLOG) < 0) {
perror("listen");
exit(EXIT_FAILURE);
}
=== 3.4 === accept() ===
int accept(int sockfd, struct sockaddr *addr, socklen_t *addrlen);
Назначение: принимает входящее подключение.
Возвращает **новый сокет**, который связан с конкретным клиентом.
В вашем коде:
if ((new_socket = accept(server_fd, (struct sockaddr *)&address, &addrlen)) < 0) {
perror("accept");
exit(EXIT_FAILURE);
}
=== 3.5 === connect() ===
int connect(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
Назначение: подключается к серверу.
Только для клиента!
В вашем коде (client.c):
if (connect(sock, (struct sockaddr*)&serv_addr, sizeof(serv_addr)) < 0) {
printf("Подключение не удалось\n");
return NULL;
}
=== 3.6 === send() ===
ssize_t send(int sockfd, const void *buf, size_t len, int flags);
Назначение: отправляет данные через сокет.
В вашем коде:
send(sock, &msg, sizeof(msg), 0);  // отправляем всю структуру msg
=== 3.7 === recv() ===
ssize_t recv(int sockfd, void *buf, size_t len, int flags);
Назначение: получает данные из сокета.
В вашем коде:
recv(sock, &msg, sizeof(msg), 0);  // получаем всю структуру msg
=== 3.8 === close() ===
int close(int fd);
Назначение: закрывает сокет.
В вашем коде:
close(sock);
-------------------------------------------------------------------------------
4. СТРУКТУРЫ ДАННЫХ ДЛЯ АДРЕСАЦИИ
-------------------------------------------------------------------------------
=== 4.1 === struct sockaddr ===
struct sockaddr {
unsigned short    sa_family;    // Семейство (AF_INET и т.п.)
char              sa_data[14];  // Адрес (IP + порт)
};
Это универсальная структура, но ей неудобно пользоваться.
=== 4.2 === struct sockaddr_in (для IP-сетей) ===
struct sockaddr_in {
short int          sin_family;  // AF_INET
unsigned short int sin_port;    // порт (в сетевом формате!)
struct in_addr     sin_addr;    // IP-адрес (в сетевом формате!)
unsigned char      sin_zero[8]; // выравнивание
};
=== 4.3 === struct in_addr ===
struct in_addr {
unsigned long s_addr;  // IP-адрес как число
};
В вашем коде (сервер):
struct sockaddr_in address;
address.sin_family = AF_INET;
address.sin_addr.s_addr = INADDR_ANY;  // "любой IP"
address.sin_port = htons(PORT);        // "порт 8989"
htons() — Host TO Network Short — преобразует порт в "сетевой порядок байт".
Это важно, потому что разные компьютеры хранят числа по-разному.
-------------------------------------------------------------------------------
5. ПОРЯДОК БАЙТ
-------------------------------------------------------------------------------
Важно: числа (порты, IP) передаются в "сетевом порядке".
- htons(port) — преобразует порт в сетевой порядок
- htonl(ip) — преобразует IP в сетевой порядок
- ntohs() — обратно
- ntohl() — обратно
В вашем коде:
address.sin_port = htons(PORT);  // обязательно!
-------------------------------------------------------------------------------
6. КАК РАБОТАЕТ СЕТЕВОЙ СЕРВЕР (шаг за шагом)
-------------------------------------------------------------------------------
=== Шаг 1: Создание сокета ===
int server_fd = socket(AF_INET, SOCK_STREAM, 0);
=== Шаг 2: Привязка к адресу ===
address.sin_family = AF_INET;
address.sin_addr.s_addr = INADDR_ANY;
address.sin_port = htons(8989);
bind(server_fd, (struct sockaddr*)&address, sizeof(address));
=== Шаг 3: Прослушивание ===
listen(server_fd, 10);  // очередь из 10 подключений
=== Шаг 4: Принятие подключений в цикле ===
while (1) {
int client_fd = accept(server_fd, ..., ...);
// Создаем поток для обработки клиента
pthread_create(..., handle_client, &client_fd);
}
=== Шаг 5: Обработка клиента в потоке ===
void* handle_client(void* arg) {
int client_fd = *(int*)arg;
// recv() -> обработка -> send()
close(client_fd);
}
-------------------------------------------------------------------------------
7. КАК РАБОТАЕТ СЕТЕВОЙ КЛИЕНТ (шаг за шагом)
-------------------------------------------------------------------------------
=== Шаг 1: Создание сокета ===
int sock = socket(AF_INET, SOCK_STREAM, 0);
=== Шаг 2: Подготовка адреса сервера ===
serv_addr.sin_family = AF_INET;
serv_addr.sin_port = htons(8989);
inet_pton(AF_INET, server_ip, &serv_addr.sin_addr);  // строка IP -> число
=== Шаг 3: Подключение ===
connect(sock, (struct sockaddr*)&serv_addr, sizeof(serv_addr));
=== Шаг 4: Отправка/получение данных ===
send(sock, &msg, sizeof(msg), 0);
recv(sock, &msg, sizeof(msg), 0);
=== Шаг 5: Закрытие ===
close(sock);
-------------------------------------------------------------------------------
8. inet_pton() — ВАЖНАЯ ФУНКЦИЯ
-------------------------------------------------------------------------------
const char *ip_str = "192.168.1.1";
struct in_addr ip_num;
inet_pton(AF_INET, ip_str, &ip_num);  // строка -> число
Используется в клиенте:
inet_pton(AF_INET, server_ip, &serv_addr.sin_addr);
-------------------------------------------------------------------------------
9. ПОЧЕМУ send/recv ПЕРЕДАЮТ sizeof(msg)?
-------------------------------------------------------------------------------
Потому что структура msg_t содержит **все** данные, которые нужно передать.
Вместо:
send(sock, &msg.type, sizeof(msg.type));
send(sock, &msg.id, sizeof(msg.id));
...
Проще и надежнее:
send(sock, &msg, sizeof(msg), 0);  // отправить всю структуру целиком
Аналогично recv.
ВАЖНО: работает только если структура одинаковая на сервере и клиенте.
-------------------------------------------------------------------------------
10. ПОТОКИ В СЕТЕВОМ СЕРВЕРЕ
-------------------------------------------------------------------------------
После accept() сервер создает **новый поток** для каждого клиента:
pthread_create(&thread, NULL, handle_client, fd_ptr);
pthread_detach(thread);  // не ждем завершения
Это позволяет серверу обслуживать **множество клиентов одновременно**.
-------------------------------------------------------------------------------
11. ПОЧЕМУ НУЖЕН mutex В СЕТЕВОЙ ВЕРСИИ?
-------------------------------------------------------------------------------
Потому что:
- Сервер создает поток на каждого клиента
- Все потоки работают с **одной общей структурой bath_s
- Без мьютекса может возникнуть race condition (гонка данных)
Поэтому:
pthread_mutex_lock(&bath_s.mutex);
// изменяем bath_s
pthread_cond_wait(&bath_s.cond, &bath_s.mutex); // освобождает мьютекс
pthread_mutex_unlock(&bath_s.mutex);
-------------------------------------------------------------------------------
12. ПОЧЕМУ SOCK_STREAM?
-------------------------------------------------------------------------------
Потому что:
- Нужна надежность: данные не должны теряться
- Порядок важен: сначала MSG_JOIN, потом MSG_LEAVE
- TCP обеспечивает это автоматически
SOCK_DGRAM (датаграммы) не подходит: могут потеряться, прийти в другом порядке.
-------------------------------------------------------------------------------
13. ПОЧЕМУ ПОРТ 8989?
-------------------------------------------------------------------------------
PORT = 8989 — это просто число, которое сервер и клиент знают заранее.
- Сервер "слушает" на порту 8989
- Клиент "подключается" к порту 8989
Можно выбрать любой доступный порт (обычно > 1024).
-------------------------------------------------------------------------------
14. ЧАСТЫЕ ОШИБКИ
-------------------------------------------------------------------------------
❌ Не проверять возвращаемые значения socket(), bind(), connect()...
✅ Всегда проверяйте: if (sock < 0) { perror("..."); exit(1); }
❌ Отправлять/получать не sizeof(struct), а по частям
✅ Лучше отправлять всю структуру целиком
❌ Не закрывать сокеты
✅ Всегда вызывайте close()
❌ Пытаться использовать один сокет сервера для общения с клиентом
✅ accept() возвращает **новый** сокет для клиента!
❌ Не использовать htons() для порта
✅ Всегда: sin_port = htons(PORT)
-------------------------------------------------------------------------------
15. ЗАКЛЮЧЕНИЕ
-------------------------------------------------------------------------------
Socket API — это основа сетевого программирования.
- socket() — создать сокет
- bind() — привязать к адресу (сервер)
- listen() — начать слушать (сервер)
- accept подключение (сервер)
- connect() — подключиться (клиент)
- send()/recv() — обмен данными
- close() — закрыть
В вашей программе:
- Сервер хранит состояние ванны и синхронизирует доступ через мьютексы
- Клиенты подключаются по сети, отправляют запросы, получают обновления
- Каждый клиент — это поток на сервере
- Все данные передаются в виде бинарной структуры msg_t
Сокеты — это мощный, но сложный инструмент. Понимание их работы важно для
любого системного программиста.
------------------------------------------------------------------------------

==============================================================================
4.2 СОКЕТЫ В ФАЙЛОВОМ ПРОСТРАНСТВЕ ИМЕН (UNIX DOMAIN SOCKETS)
================================================================================
Эта часть объясняет, как работают сокеты, использующие файловую систему
для адресации (в отличие от IP-адресов и портов). Такие сокеты работают
только на **одном компьютере** и не могут использоваться для сетевых
соединений между разными машинами.
-------------------------------------------------------------------------------
1. ЧТО ТАКОЕ UNIX DOMAIN SOCKETS?
-------------------------------------------------------------------------------
Unix DomainS) — это сокеты, которые используют **файловые пути**
в качестве адресов, а не IP-адреса и порты.
- Работают только между процессами на **одном компьютере**
- Быстрее сетевых сокетов (нет сетевого стека)
- Используют файловую систему для именования (например, "/tmp/myservice.sock")
- Не могут использоваться для связи между разными машинами, даже если
файловую систему монтирует другой компьютер (NFS и т.п.)
В вашем коде (сетевая версия ванной) они **не используются**.
-------------------------------------------------------------------------------
2. АТРИБУТЫ UNIX СОКЕТО=== 2.1 === ДОМЕН ===
AF_UNIX — домен для сокетов в файловом пространстве имен.
В отличие от AF_INET (IP-сети), AF_UNIX работает только локально.
=== 2.2 === ТИП === — надежный поток (как TCP, но локально)
- SOCK_DGRAM — датаграммы (как UDP, но локально)
В примере из лекции: SOCK_DGRAM (ненадежные, но в файловом пространстве
они тоже надежны).
=== 2.3 === ПРОТОКОЛ ===
Обычно 0 — автоматический выбор.
-------------------------------------------------------------------------------
3. ФУНКЦИИ РАБОТЫ С UNIX СОКЕТАМИ
-------------------------------------------------------------------------------
=== 3.1 === socket() ===
int sock = socket(AF_UNIX, SOCK_DGRAM, 0);
Создает сокет в домене AF_UNIX.
=== 3.2 === struct sockaddr_un ===
struct sockaddr_un {
sa_family_t sun_family;  // AF_UNIX
char        sun_path[108]; // путь к файлу-сокету
};
Это специализированная структура для Unix сокетов. Удобнее, чем sockaddr.
В примере лекции использовали общую struct sockaddr:
struct sockaddr srvr_name;
srvr_name.sa_family = AF_UNIX;
strcpy(srvr_name.sa_data, "socket.soc"); // <-- старый стиль, не рекомендуется
// Лучше использовать sockaddr_un:
struct sockaddr_un srvr_name;
srvr_name.sun_family = AF_UNIX;
strcpy(srvr_name.sun_path, "socket.soc"); // <-- правильный путь
=== 3.3 === bind() ===
int bind(int sockfd, const struct sockaddr *addr, socklen_t addrlen);
Для Unix сокета:
bind(sock, (struct sockaddr*)&srvr_name, sizeof(srvr_name));
После этого создается файл "socket.soc" в текущей директории.
=== 3.4 === sendto() и recvfrom() ===
Для датаграммных сокетов (SOCK_DGRAM) часто используются эти функции:
ssize_t sendto(int sockfd, const void *buf, size_t len, int flags,
const struct sockaddr *dest_addr, socklen_t dest_len);
ssize_t recvfrom(int sockfd, void *buf, size_t len, int flags,
struct sockaddr *src_addr, socklen_t *src_len);
- sendto(): отправить данные конкретному адресату (указываем адрес каждый раз)
- recvfrom(): получить данные и узнать, откуда они пришли
В отличие от send/recv (д эти функции работают с
**отдельными сообщениями** (датаграммами).
=== 3.5 === connect() (опционально для SOCK_DGRAM) ===
Для датаграммных сокетов можно вызвать connect(), чтобы задать
**постоянный адрес назначения**. Тогда можно использовать send/recv
вместо sendto/recvfrom.
=== 3.6 === close() ===
Закрывает сокет (как и для сетевых).
=== 3.7 === unlink() ===
int unlink(const char *pathname);
После завершения сервера нужно **удалить файл сокета** вручную!
unlink("socket.soc");
Иначе при следующем запуске сервера может быть ошибка "Address already in use".
-------------------------------------------------------------------------------
4. ПРИМЕР ИЗ ЛЕКЦИИ (СЕРВЕР: fsserver.c)
-------------------------------------------------------------------------------
#include <sys/types.h>
#include <sys/socket.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#define SOCK_NAME "int main() {
int sock;
struct sockaddr srvr_name; // <-- лучше использовать struct sockaddr_un
sock = socket(AF_UNIX, SOCK_DGRAM, 0);
if (sock < 0) {
perror("socket failed");
return EXIT_FAILURE;
}

srvr_name.sa_family = AF_UNIX;
strcpy(srvr_name.sa_data, SOCK_NAME); // <-- небезопасно, лучше sun_path

if (bind(sock, &srvr_name, SUN_LEN(&srvr_name)) < 0) { // <-- SUN_LEN() не всегда доступен
// Лучше так:
// struct sockaddr_un srvr_name_un;
// srvr_name_un.sun_family = AF_UNIX;
// strcpy(srvr_name_un.sun_path, SOCK_NAME);
// bind(sock, (struct sockaddr*)&srvr_name_un, sizeof(srvr_name_un));
perror("bind failed");
return EXIT_FAILURE;
}

char buf[1024];
struct sockaddr rcvr_name; // <-- для получения адреса клиента (неинформативен для UDS)
socklen_t namelen = sizeof(rcvr_name);

int bytes = recvfrom(sock, buf, sizeof(buf), 0, &rcvr_name, &namelen);
if (bytes > 0) {
buf[bytes] = '\0'; // добавляем терминатор
printf("Получено: %s\n", buf);
}

close(sock);
unlink(SOCK_NAME); // <-- важно удалить файл!

return 0;
}
=== ПОЯСНЕНИЯ К КОДУ ===
- socket(AF_UNIX, SOCK_DGRAM, 0) — создаем датаграммный Unix сокет
- srvr_name.sa_family = AF_UNIX — указываем домен
- strcpy(srvr_name.sa_data, "socket.soc") — устанавливаем имя файла
- bind() — привязываем сокет к файлу "socket.soc"
- recvfrom() — получаем сообщение от клиента
- close() — закрываем сокет
- unlink() — удаляем файл сокета
-------------------------------------------------------------------------------
5. ПРИМЕР ИЗ ЛЕКЦИИ (КЛИЕНТ: fsclient.c)
-------------------------------------------------------------------------------
#include <sys/types.h>
#include <sys/socket.h>
#include <string.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

#define SOCK_NAME "socket.soc"

int main() {
int sock;
struct sockaddr srvr_name; // <-- лучше sockaddr_un
sock = socket(AF_UNIX, SOCK_DGRAM, 0);
if (sock < 0) {
perror("socket failed");
return EXIT_FAILURE;
}

srvr_name.sa_family = AF_UNIX;
strcpy(srvr_name.sa_data, SOCK_NAME);

char buf[] = "Hello, Unix sockets!";
int sent = sendto(sock, buf, strlen(buf), 0,
&srvr_name, sizeof(srvr_name));
if (sent < 0) {
perror("sendto failed");
} else {
printf("Отправлено %d байт\n", sent);
}

close(sock);
return 0;
}
=== ПОЯСНЕНИЯ К КОДУ ===
- socket(AF_UNIX, SOCK_DGRAM, 0) — создаем сокет
- Заполняем структуру адреса сервера
- sendto() — отправляем сообщение на указанный адрес (файл)
- close() — закрываем сокет
-------------------------------------------------------------------------------
6. ПРЕИМУЩЕСТВА UNIX СОКЕТОВ
-------------------------------------------------------------------------------
- Очень быстрые (быстрее сетевых, нет сетевого стека)
- Простота адресации (файловый путь)
- Поддерживают оба типа: SOCK_STREAM и SOCK_DGRAM
- Встроенный механизм безопасности файловой системы (владелец, права)
-------------------------------------------------------------------------------
7. НЕДОСТАТКИ UNIX СОКЕТОВ
-------------------------------------------------------------------------------
- Работают только локально (не для сетей)
- Требуют управления файлами (создание, удаление)
- Могут столкнуться с проблемами прав доступа к файлу
-------------------------------------------------------------------------------
8. КОГДА ИСПОЛЬЗОВАТЬ UNIX СОКЕТЫ?
-------------------------------------------------------------------------------
- Межпроцессное взаимодействие на одном компьютере
- Когда нужна надежность (SOCK_STREAM) или датаграммы (SOCK_DGRAM)
- Когда сетевые сокеты избыточны
- Внутренние сервисы (например, X11, Docker daemon используют UDS)
- Не подходит для вашной (там сетевые сокеты для связи)
-------------------------------------------------------------------------------
9. ЧАСТЫЕ ОШИБКИ
-------------------------------------------------------------------------------
❌ Забыть unlink() — файл сокета остается в системе
✅ Всегда вызывать unlink() в конце сервера
❌ Неправильно рассчитать длину структуры в bind/sendto
✅ Использовать sizeof(struct sockaddr_un)
❌ Использовать strcpy для sa_data — небезопасно
✅ Лучше использовать struct sockaddr_un и sun_path
❌ Пытаться использовать UDS для сетевого взаимодействия
✅ UDS только локально!
❌ Не обработать ошибки recvfrom/sendto
✅ Всегда проверять возвращаемые значения
-------------------------------------------------------------------------------
10. СРАВНЕНИЕ С СЕТЕВЫМИ СОКЕТАМИ
-------------------------------------------------------------------------------
||| Сетевые (AF_INET) |||
- Работают по сети
- Используют IP + порт
- SOCK_STREAM = TCP (надежный)
- SOCK_DGRAM = UDP (ненадежный)
- Подходят для вашей программы ванной
||| Unix (AF_UNIX) |||
- Работают локально
- Используют файловый путь
- SOCK_STREAM = надежный
- SOCK_DGRAM = надежный (!)
- Не подходят для вашей программы ванной (нужна сеть)
-------------------------------------------------------------------------------
11. ЗАКЛЮЧЕНИЕ
-------------------------------------------------------------------------------
Unix Domain Sockets — мощный и эффективный способ межпроцессного
взаимодействия на одном компьютере. Они проще сетевых сокетов в плане
адресации и быстрее, но ограничены локальной машиной. Ваша программа
сетевой ванной комнаты использует AF_INET, так как предполагает
взаимодействие по сети, а не локально.
------------------------------------------------------------------------------

==============================================================================
4.3 ПАРНЫЕ СОКЕТЫ (SOCKET PAIRS)
================================================================================
Эта часть объясняет, как работают парные сокеты (socket pairs) — механизм
межпроцессного взаимодействия, похожий на неименованные каналы (pipes),
но использующий API сокетов. Они создаются парами и подходят для общения
между родительским и дочерним процессом.
-------------------------------------------------------------------------------
1. ЧТО ТАКОЕ ПАРНЫЕ СОКЕТЫ?
-------------------------------------------------------------------------------
Парные сокеты (socket pairs) — это **два связанных сокета**, создаваемых
одновременно функцией socketpair(). Они:
- Не имеют имени (в отличие от Unix Domain Sockets в файловом пространстве)
- Работают только между **связанными процессами** (обычно родитель-дочерний)
- Предназначены для **двусторонней** передачи данных (full-duplex)
- Являются альтернативой неименованным каналам (pipe()), но используют
интерфейс сокетов (send/recv или read/write)
В вашем коде (сетевая ванна) они **не используются**.
-------------------------------------------------------------------------------
2. АТРИБУТЫ ПАРНОГО СОКЕТА
-------------------------------------------------------------------------------
=== 2.1 === ДОМЕН ===
AF_UNIX — только для локального взаимодействия.
socketpair() поддерживает только AF_UNIX.
=== 2.2 === ТИП ===
- SOCK_STREAM — надежный поток байтов (как TCP, но локально)
- SOCK_DGRAM — датаграммы (редко используется)
В примере: SOCK_STREAM
=== 2.3 === ПРОТОКОЛ ===
Обычно 0 — автоматический выбор.
-------------------------------------------------------------------------------
3. ФУНКЦИИ РАБОТЫ С ПАРНЫМИ СОКЕТАМИ
-------------------------------------------------------------------------------
=== 3.1 === socketpair() ===
int socketpair(int domain, int type, int protocol, int sv[2]);
Назначение: создает пару связанных сокетов.
Параметры:
- domain: AF_UNIX (единственный поддерживаемый)
- type: SOCK_STREAM или SOCK_DGRAM
- protocol: обычно 0
- sv: массив из 2-х int, куда записываются дескрипторы
Возвращаем 0 при успехе
- -1 при ошибке
Пример из лекции:
int sockets[2];
if (socketpair(AF_UNIX, SOCK_STREAM, 0, sockets) < 0) {
perror("socketpair() failed");
return EXIT_FAILURE;
}
После вызова:
- sockets[0] и sockets[1] — два связанных сокета
- Данные, записанные в sockets[0], можно прочитать из sockets[1] и наоборот
=== 3.2 === read() / write() или send() / recv() ===
После создания пары можно использовать стандартные функции ввода-вывода:
- read(sockets[0], buffer, size) <- данные пришли из sockets[1]
- write(sockets[0], buffer, size) -> данные ушли в sockets[1]
или
- recv(sockets[0], buffer, size, flags)
- send(sockets[0], buffer, size, flags)
=== 3.3 === close() ===
Закрывает один из сокетов в паре.
Важно: закрытие одного сокета в паре не закрывает другой.
-------------------------------------------------------------------------------
4. ПРИМЕР ИЗ ЛЕКЦИИ (sockpair.c)
-------------------------------------------------------------------------------
#include <sys/types.h>
#include <sys/socket.h>
#include <stdlib.h>
#include <stdio.h>
#include <errno.h>
#include <unistd.h> // для fork, read, write, close

#define STR1 "How are you?"
#define STR2 "I'm ok, thank you."
#define BUF_SIZE 1024

int main(int argc, char ** argv)
{
int sockets[2];
char buf[BUF_SIZE];
int pid;

if (socketpair(AF_UNIX, SOCK_STREAM, 0, sockets) < 0) {
perror("socketpair() failed");
return EXIT_FAILURE;
}

pid = fork(); // создаём дочерний процесс

if (pid != 0) { // РОДИТЕЛЬСКИЙ процесс
close(sockets[1]); // закрет в паре

write(sockets[0], STR1, sizeof(STR1)); // отправляем сообщение
read(sockets[0], buf, sizeof(buf));    // читаем ответ
printf("%s\n", buf);                   // печатаем ответ

close(sockets[0]); // закрываем свой сокет
} else { // ДОЧЕРНИЙ процесс
close(sockets[0]); // закрываем первый сокет в паре

read(sockets[1], buf, sizeof(buf));    // читаем сообщение
printf("%s\n", buf);                   // печатаем сообщение
write(sockets[1], STR2, sizeof(STR2)); // отправляем ответ

close(sockets[1]); // закрываем свой сокет
}
return 0;
}
=== ПОЯСНЕНИЯ К КОДУ ===
- socketpair() создает два связанных сокета: sockets[0] и sockets[1]
- fork() разделяет выполнение на два процесса
- Родитель:
- Закрывает sockets[1] (не нужен)
- Использует sockets[0] для отправки и получения
- Дочерний:
- Закрывает sockets[0] (не нужен)
- Использует sockets[1] для получения и отправки
- Оба процесса обмениваются сообщениями через сокеты
- close() закрывает используемый сокет в каждом процессе
-------------------------------------------------------------------------------
5. ПОЧЕМУ socketpair() СЧИТАЕТСЯ СОКЕТОМ?
-------------------------------------------------------------------------------
- Интерфейс: использует системный вызов socketpair, который принимает
аргументы домена и типа (как socket())
- Реализация: внутри ядра это полноценные сокеты, просто связанные друг с другом
- Возможности: можно использовать send/recv, apply к ним мьютексы,
они участвуют в select/poll/epoll
- Однако:
- Не имеют адреса (не нужно bind/connect)
- Не работают в модели клиент-сервер
- Предназначены для связи между родственными процессами
-------------------------------------------------------------------------------
6. ПРЕИМУЩЕСТВА ПАРНЫХ СОКЕТОВ ПЕРЕД PIPE()
-------------------------------------------------------------------------------
- Поддерживают **двустороннюю** передачу сразу (full-duplex)
- Можно использовать **сокетные функции** (send/recv с флагами)
- Поддерживают **срочные данные** (MSG_OOB) (в теории)
- Могут использоваться в **select/poll/epoll** наравне с другими сокетами
- Более "унифицированный" интерфейс, если основной код уже использует сокеты
-------------------------------------------------------------------------------
7. НЕДОСТАТКИ ПАРНЫХ СОКЕТОВ
-------------------------------------------------------------------------------
- Работают **только локально** (AF_UNIX)
- Требуют **два дескриптора** (в отличие от pipe с одним массивом)
- Меньше документации и примеров, чем у pipe()
- Немного больше накладных расход pipe()
-------------------------------------------------------------------------------
8. КОГДА ИСПОЛЬЗОВАТЬ ПАРНЫЕ СОКЕТЫ?
-------------------------------------------------------------------------------
- Когда нужно межпроцессное взаимодействие "точка-точка" между родственными процессами
- Когда нужна **двусторонняя** связь (и pipe неудобен)
- Когда остальной код уже использует сокетный API (удобство)
- Альтернатива неименованному каналу (pipe), если нужен full-duplex
- В системах, где хочется унифицировать обработку разных типов дескрипторов
(например, через select)
- Не подходит для вашей программы ванной (там сетевые сокеты для связи)
-------------------------------------------------------------------------------
9. ЧАСТЫЕ ОШИБКИ
-------------------------------------------------------------------------------
❌ Не закрыть неиспользуемый дескриптор в каждом процессе
✅ После fork() каждый процесс должен close() неиспользуемый сокет из пары
❌ Пытаться использовать socketpair с AF_INET
✅ socketpair поддерживает только AF_UNIX
❌ Считать, что это сетевой сокет
✅ socketpair — только локально
❌ Не проверить возвращаемое значение socketpair
✅ Всегда проверять: if (socketpair(...) < 0) { ... }
❌ Перепутать, какой дескриптор используется в каком процессе
✅ Ясно указать: родитель использует sockets[0], дочерний sockets[1] (или наоборот)
-------------------------------------------------------------------------------
10. СРАВНЕНИЕ: pipe() vs socketpair()
-------------------------------------------------------------------------------
||| pipe() |||
- Создает односторонний канал (два дескриптора: read-end, write-end)
- read(fd[0]), write(fd[1])
- Типично: один процесс читает, другой пишет
- Проще для односторонней связи
||| socketpair() |||
- Создает двустороннюю пару (два сокета)
- read(sock[0]), write(sock[1]) и наоборот
- Оба процесса могут и читать, и писать через свой сокет
- Удобнее для двусторонней связи между родственниками
-------------------------------------------------------------------------------
11. ЗАКЛЮЧЕНИЕ
-------------------------------------------------------------------------------
Парные сокеты — это удобный способ организации двустороннего канала
между родственными процессами (обычно родитель-дочерний), использующий
API сокетов. Они похожи на неименованные каналы, но обеспечивают
full-duplex связь и совместимы с сокетным интерфейсом. В вашей программе
сетевой ванной комнаты они **не используются**, так как там нужна
связь по сети (AF_INET).
------------------------------------------------------------------------------*/

==============================================================================
4.4 СЕТЕВЫЕ СОКЕТЫ — ТЕОРИЯ И РАЗБОР
================================================================================
Эта часть объясняет, как работают сетевые сокеты — основной способ
взаимодействия между процессами по сети (включая локальную сеть).
-------------------------------------------------------------------------------
1. ЧТО ТАКОЕ СЕТЕВЫЕ СОКЕТЫ?
-------------------------------------------------------------------------------
Сетевые сокеты — это сокеты, работающие через IP-сети.
- Используют домен AF_INET (IPv4) или AF_INET6 (IPv6)
- Адресуются IP-адресом + портом
- Могут работать как локально (127.0.0.1), так и по сети
- Используют протоколы TCP (SOCK_STREAM) или UDP (SOCK_DGRAM)
- Основной способ связи между разными машинами
В вашей программе сетевой ванной комнаты они **используются**!
-------------------------------------------------------------------------------
2. ПРЕИМУЩЕСТВА И НЕДОСТАТКИ СЕТЕВЫХ СОКЕТОВ
-------------------------------------------------------------------------------
=== ПРЕИМУЩЕСТВА ===
- Универсальность: подходят как для локальной, так и для сетевой связи
- Если сегодня приложение использует сетевые сокеты локально,
то легко масштабируется на несколько машин в будущем
- Стандартизированы, поддержка во всех ОС
=== НЕДОСТАТКИ ===
- Медленнее, чем Unix Domain Sockets или разделяемая память,
так как данные проходят через сетевой стек (даже локально)
- Более сложный код (адресация, подключение)
-------------------------------------------------------------------------------
3. ФУНКЦИИ РАБОТЫ С СЕТЕВЫМИ СОКЕТАМИ
-------------------------------------------------------------------------------
 === socket() ===
int sock = socket(AF_INET, SOCK_STREAM, 0);
- AF_INET: сетевой сокет IPv4
- SOCK_STREAM: надежное соединение (TCP)
- 0: автоматический выбор протокола (TCP)
=== 3.2 === struct sockaddr_in ===
struct sockaddr_in {
short int          sin_family;  // AF_INET
unsigned short int sin_port;    // порт (в сетевом порядке!)
struct in_addr     sin_addr;    // IP-адрес (в сетевом порядке!)
unsigned char      sin_zero[8]; // выравнивание
};
Используется для задания адреса сервера/клиента.
=== 3.3 === htons() ===
unsigned short htons(unsigned short hostshort);
Преобразует порт из порядка хоста в сетевой.
Важно: sin_port = htons(PORT);
=== 3.4 === bind() ===
bind(sock, (struct sockaddr *) &serv_addr, sizeof(serv_addr));
Привязывает сокет к IP-адресу и порту.
Только для сервера!
serv_addr.sin_addr.s_addr = INADDR_ANY; // слушать на всех интерфейсах
serv_addr.sin_port = htons(port);       // обязательно в сетевом порядке!
=== 3.5 === listen() ===
listen(sock, backlog);
Перокет в режим ожидания подключений.
backlog: размер очереди входящих соединений.
=== 3.6 === accept() ===
int newsock = accept(sock, (struct sockaddr *) &cli_addr, &clen);
Принимает входящее подключение.
- Возвращает **новый** сокет, связанный с клиентом
- Исходный сокет sock остается в режиме прослушивания
- cli_addr: адрес клиента (можно NULL)
=== 3.7 === connect() ===
connect(sock, &serv_addr, sizeof(serv_addr));
Клиент подключается к серверу.
serv_addr: адрес сервера (IP + порт)
=== 3.8 === read() / write() ===
Можно использовать вместо send/recv.
read(sock, buffer, size);
write(sock, buffer, size);
=== 3.9 === close() ===
Закрывает сокет.
-------------------------------------------------------------------------------
4. СЕРВЕР (netserver.c) — ПОШАГОВО
-------------------------------------------------------------------------------
=== Шаг 1: Создание сокета ===
int sock = socket(AF_INET, SOCK_STREAM, 0);
=== Шаг 2: Подготовка адреса сервера ===
struct sockaddr_in serv_addr;
serv_addr.sin_family = AF_INET;
serv_addr.sin_addr.s_addr = INADDR_ANY; // любой IP
serv_addr.sin_port = htons(port);       // порт в сетевом порядке
=== Шаг 3: Привязка ===
bind(sock, (struct sockaddr *) &serv_addr, sizeof(serv_addr));
=== Шаг 4: Прослушивание ===
listen(sock, 1); // очередь из 1 подключения
=== Шаг 5: Принятие подключения ===
int newsock = accept(sock, NULL, NULLокет для клиента
=== Шаг 6: Обмен данными ===
read(newsock, buffer, size);
write(newsock, response, size);
=== Шаг 7: Закрытие ===
close(newsock); // закрываем сокет клиента
close(sock);    // закрываем слушающий сокет
=== ВАЖНО ===
- accept() возвращает **новый** сокет (newsock)
- Оригинальный сокет (sock) остается слушать
- Сервер может обслуживать несколько клиентов, создавая потоки/процессы
(в примере — один клиент)
-------------------------------------------------------------------------------
5. КЛИЕНТ (netclient.c) — ПОШАГОВО
-------------------------------------------------------------------------------
=== Шаг 1: Преобразование имени сервера в IP (опционально) ===
struct hostent *server = gethostbyname(hostname_or_ip);
if (server == NULL) { /* ошибка */ }
// hostname_or_ip — строка: "localhost", "127.0.0.1", "example.com"
// server->h_addr — IP-адрес в бинарном виде
=== Шаг 2: Подготовка адреса сервера ===
struct sockaddr_in serv_addr;
serv_addr.sin_family = AF_INET;
serv_addr.sin_port = htons(port);
// Копируем IP-адрес из gethostbyname
strncpy((char *)&serv_addr.sin_addr.s_addr, (char *)server->h_addr, server->h_length);
// Или если IP известен как строка:
// inet_pton(AF_INET, "192.168.1.1", &serv_addr.sin_addr);
=== Шаг 3: Создание сокета ===
int sock = socket(AF_INET, SOCK_STREAM, 0);
=== Шаг 4: Подключение к серверу ===
connect(sock, &serv_addr, sizeof(serv_addr));
=== Шаг 5: Обмен данными ===
write(sock, request, strlen(request));
read(sock, response, size);
=== Шаг 6: Закрытие ===
close(sock);
-------------------------------------------------------------------------------
6. gethostbyname() — ПРЕОБРАЗОВАНИЕ ИМЕНИ В АДРЕС
-------------------------------------------------------------------------------
struct hostent *gethostbyname(const char *name);
Назначение: преобразует имя хоста ("google.com", "localhost") в IP-адрес.
Возвращаемое значение:
- NULL: ошибка (имя не найдено)
- Указатель на struct hostent:
struct hostent {
char  *h_name;      // официальное имя хоста
char **h_aliases;   // псевдонимы
int    h_addrtype;  // AF_INET
int    h_length;    // длина адреса (обычно 4 для IPv4)
char **h_addr_list; // список IP-адресов
};
- h_addr_list[0] — первый IP-адрес
- h_addr — макрос: #define h_addr h_addr_list[0]
Использование:
struct hostent *server = gethostbyname("localhost");
memcpy(&serv_addr.sin_addr.s_addr, server->h_addr, server->h_length);
=== ЗАМЕЧАНИЕ ===
gethostbyname() устарела (not thread-safe). Современный код использует getaddrinfo().
-------------------------------------------------------------------------------
7. БЛОКИРУЮЩИЕ И НЕБЛОКИРУЮЩИЕ СОКЕТЫ
-------------------------------------------------------------------------------
=== Блокирующие (по умолчанию) ===
- read()/recv(): ждут данные
- write()/send(): ждут, пока буфер освободится
- connect(): ждет установки соединения
- accept(): ждет подключения
=== Неблокирующие ===
- Немедленно возвращают управление
- Если операция невозможна: возвращают -1 и errno = EAGAIN/EWOULDBLOCK
=== Установка неблокирующего режима ===
#include <fcntl.h>
int flags = fcntl(sock, F_GETFL, 0);
fcntl(sock, F_SETFL_NONBLOCK);
=== Использование неблокирующего сокета ===
int n = read(sock, buffer, size);
if (n == -1 && (errno == EAGAIN || errno == EWOULDBLOCK)) {
// Данных нет, повторите позже
} else if (n > 0) {
// Успешно прочитано n байт
}
=== select() — ОПРОС НЕСКОЛЬКИХ СОКЕТОВ ===
int select(int nfds, fd_set *readfds, fd_set *writefds, fd_set *exceptfds,
struct timeval *timeout);
Назначение: проверить, готовы ли сокеты к чтению/записи.
Параметры:
- nfds: максимальный номер дескриптора + 1
- readfds: набор сокетов для проверки на готовность к чтению
- writefds: для записи
- exceptfds: для исключительных ситуаций
- timeout: время ожидания (NULL = бесконечно)
Возврат: кол-во готовых дескрипторов или -1 при ошибке.
=== Макросы для работы с fd_set ===
FD_ZERO(&set);ить набор
FD_SET(sock, &set);   // добавить сокет в набор
FD_CLR(sock, &set);   // удалить сокет из набора
FD_ISSET(sock, &set); // проверить, есть ли сокет в наборе
=== Пример использования select() ===
fd_set read_set;
struct timeval tv;
FD_ZERO(&read_set);
FD_SET(sock, &read_set);
tv.tv_sec = 1;
tv.tv_usec = 500000; // 1.5 секунды
int activity = select(sock + 1, &read_set, NULL, NULL, &tv);
if (activity > 0 && FD_ISSET(sock, &read_set)) {
// Сокет готовread(sock, buffer, size);
}
=== ЗАМЕЧАНИЕ ===
select() устарел для высоконагруженных систем. Современные альтернативы:
poll(), epoll (Linux), kqueue (BSD/macOS).
-------------------------------------------------------------------------------
8. СРАВНЕНИЕ С ВАШЕЙ ПРОГРАММОЙ СЕТЕВОЙ ВАННОЙ
-------------------------------------------------------------------------------
||| netserver.c/netclient.c (из лекции) |||
- Простой сервер: обслуживает одного клиента
- Блокирующие сокеты
- Обмен текстовыми строками
- Один цикл обработки: accept -> read -> write -> close
||| Ваша сетевая ванна |||
- Сервер: принимает многих клиентов через accept + pthread_create
- Клиент: запускает много потоков-студентов
- Обмен бинарными структурами (msg_t)
- Сложная логика синхронизации на сервере (мьютексы, условные переменные)
- Использует send/recv вместо read/write (эквивалентно в данном случае)
-------------------------------------------------------------------------------
9. ЧАСТЫЕ ОШИБКИ
-------------------------------------------------------------------------------
❌ Не использовать htons() для порта
✅ serv_addr.sin_port = htons(PORT);
❌ Перепутать, какой сокет используется для чего (слушающий vs клиентский)
✅ accept() возвращает новый сокет для общения с клиентом
❌ Не проверять возвращаемые значения функций
✅ Проверять: if (socket < 0) { ... }, if (connect(...) < 0) { ... }
❌ Использовать неблокирующий сокет без проверки errno
✅ Проверять errno на EAGAIN/EWOULDBLOCK
❌ Забыть #include <arpa/inet.h> для htons, inet_pton
✅ Всегда включать нужные заголовки
❌ Пытаться использовать сокет после закрытия
✅ После close() дескриптор становится недействительным
-------------------------------------------------------------------------------
10. ЗАКЛЮЧЕНИЕ
-------------------------------------------------------------------------------
Сетевые сокеты — основной способ межпроцессного взаимодействия через сеть.
- Используют IP-адреса и порты
- Поддерживают TCP (надежный) и UDP (ненадежный)
- Работают как локально, так и по сети
- Требуют больше кода, чем UDS или pipes, но более универсальны
- В вашей программе сетевой ванной они используются для связи между
клиентом (студенты) и сервером (управление ванной).
Это ключ к распределенному взаимодействию.
------------------------------------------------------------------------------

 * ПОРТ — ТЕОРИЯ И РАЗБОР
 *
 * ========================================================================
 * 1. ЧТО ТАКОЕ ПОРТ?
 * ========================================================================
 * Порт — это **логический адрес** в пределах IP-адреса, используемый
 * для идентификации конкретного сетевого сервиса или процесса на
 * компьютере в сети.
 *
 * Представьте IP-адрес как адрес дома (192.168.1.100).
 * Порт — это как номер квартиры в этом доме (например, 80, 443, 8989).
 * IP-адрес доставляет пакет на нужный компьютер,
 * порт указывает, *какому процессу* (приложению) на этом компьютере
 * предназначен пакет.
 *
 * В IPv4 порт — это 16-битное число (от 0 до 65535).
 *
 * ========================================================================
 * 2. КАК РАБОТАЕТ ПОРТ?
 * ========================================================================
 * - Когда клиент хочет связаться с сервером, он указывает IP-адрес сервера
 *   и **номер порта**, к которому подключен сервис (например, веб-сервер на 80).
 * - Пакет отправляется на IP-адрес, но на целевом компьютере операционная
 *   система использует **номер порта**, чтобы доставить данные нужному процессу.
 * - Процесс (например, ваш сервер ванной комнаты) должен быть "привязан"
 *   (bind) к этому порту, чтобы получать данные.
 *
 * В вашем коде (server.c):
 * #define PORT 8989
 * address.sin_port = htons(PORT); // Указываем порт 8989
 * bind(server_fd, (struct sockaddr *)&address, sizeof(address)); // Привязываем сокет к порту
 *
 * ========================================================================
 * 3. КАК ОПРЕДЕЛИТЬ, КАКОЙ ПОРТ ОТКРЫТ / ЗАНЯТ?
 * ========================================================================
 * Существуют системные утилиты для просмотра сетевых соединений и портов:
 *
 * --- Linux ---
 * `netstat -tulnp` — показывает TCP/UDP прослушиваемые порты и процессы
 * - `-t`: TCP
 * - `-u`: UDP
 * - `-l`: только прослушиваемые (LISTEN)
 * - `-n`: числовые IP/PORT (без DNS/resolv)
 * - `-p`: показать PID и имя процесса (нужны права root или владельца)
 *
 * `ss -tulnp` — современная альтернатива netstat
 *
 * `lsof -i :<port>` — показывает, какой процесс использует конкретный порт
 * Пример: lsof -i :8989
 *
 * --- Windows ---
 * `netstat -anb` — показывает порты и имена процессов
 * `Get-NetTCPConnection` (PowerShell) — для TCP-соединений
 *
 * ========================================================================
 * 4. КАК ВЫДЕЛЯЮТСЫ ПОД ПРИЛОЖЕНИЯ ОС?
 * ========================================================================
 * --- Привязка (bind) ---
 * - Приложение (сервер) вызывает `bind()` с указанием порта.
 * - ОС проверяет, *свободен ли* порт.
 * - Если свободен, ОС "резервирует" порт за этим процессом.
 * - Теперь только этот процесс может принимать данные на этот порт.
 * - Если порт занят другим процессом, `bind()` возвращает ошибку "Address already in use".
 *
 * --- Системные порты (0-1023) ---
 * - Порты 0-1023 называются "привилегированными".
 * - Привязаться к ним может только процесс с правами root (или CAP_NET_BIND_SERVICE).
 * - Примеры: SSH (22), HTTP (80), HTTPS (443).
 *
 * --- Пользовательские порты (1024-49151) ---
 * - Обычно могут использоваться любыми приложениями без привилегий.
 * - Ваш `PORT 8989` из примера попадает сюда.
 *
 * --- Динамические/эфемерные порты (49152-65535) ---
 * - Используются ОС автоматически для клиентских сокетов.
 * - Когда клиент вызывает `connect()` без предварительного `bind()`,
 *   ОС автоматически выбирает свободный порт из этого диапазона.
 * - В `client.c` вашей программы порт клиента выбирается ОС автоматически.
 *
 * --- Поведение ОС при привязке ---
 * - `INADDR_ANY` (0.0.0.0): сервер слушает на *всех* сетевых интерфейсах.
 *   В `server.c`: address.sin_addr.s_addr = INADDR_ANY;
 * - Конкретный IP: сервер слушает только на указанном интерфейсе.
 * - `SO_REUSEADDR` (в вашем коде): позволяет повторно использовать порт
 завершения сервера (важно при отладке).
 *
 * int opt = 1;
 * setsockopt(server_fd, SOL_SOCKET, SO_REUSEADDR, &opt, sizeof(opt));
 *
 * ========================================================================
 * 5. СВЯЗЬ С ВАШЕЙ ПРОГРАММОЙ СЕТЕВОЙ ВАННОЙ
 * ========================================================================
 * - Сервер (`server.c`) "прослушивает" (listen) на `PORT 8989`.
 * - Клиенты (`client.c`) "подключаются" (connect) к IP-адресу сервера и порту 8989.
 * - ОС направляет входящие соединения на процесс, который вызвал `bind(8989)`.
 * - Если вы запустите второй экземпляр сервера, `bind()` на 8989 у второго
 *   завершится ошибкой, так как порт уже занят первым.
 *
 * ========================================================================
 * 6. ВАЖНОСТЬ htons()
 * ========================================================================
 * Порты передаются в сетевом порядке байт.
 * `htons(PORT)` — Host TO Network Short — преобразует порт в правильный порядок.
 * В `server.c` и `client.c`:
 * serv_addr.sin_port = htons(PORT);
 * Это критично для корректной работы на разных архитектурах.
 *
 * ========================================================================
 * 7. ЗАКЛЮЧЕНИЕ
 * ========================================================================
 * Порт — это ключевой элемент сетевой адресации на уровне TCP/UDP.
 * Он позволяет нескольким приложениям на одном компьютере работать
 * в сети одновременно, не мешая друг другу.
 * ОС управляет портами, позволяя приложениям привязываться к ним
 * и направляя сетевой трафик нужному процессу.
 * Понимание работы портов важно для отладки сетевых приложений.
 */
